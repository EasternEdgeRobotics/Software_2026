# Firmware

## LapisLazuli

The [LapisLazuli](./LapisLazuli/) folder contains the firmware for the RP2040 onboard Bluestar's tooling board (called LapisLazuli). It includes code for interfacing with the topides code (see [ros_workpace](../ros_workspace/README.md)) via UART and controlling Bluestar's peripherals.